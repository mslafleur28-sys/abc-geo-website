# Draft version backups

Per-save snapshots for article briefs live here:

`{slug}/{timestamp}.md` (or `.json`)

- Created automatically on every draft save
- Snapshots older than 7 days are pruned on the next save for that slug
- Current drafts stay in `content/drafts/` or `content/published/` — this folder
  is only history
