# Published article briefs

Drafts marked **Published** from `/admin/drafts` move here as `{slug}.md`
(or `.json`). Status in frontmatter is `published`.

This is an editorial archive of the brief — the live article HTML still lives
under `blog/{slug}.html` after a Cursor Agent (or manual) publish step.
